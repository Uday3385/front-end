import { primary, error, secondary, borderColor } from './colors';

export default function createPalette() {
    return {
        borderColor,
        secondary,
        primary,
        error,
    };
}
